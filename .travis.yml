language: node_js
dist: focal
node_js:
  - "20.11.0"
  before_install:
  - npm install -g npm@latest
script:
  - npm test
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  local_dir: build
  on:
    branch: main
